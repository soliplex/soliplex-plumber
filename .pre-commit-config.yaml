repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version matching your devenv setup
    rev: v0.16.6
    hooks:
      - id: ruff-check
        types: [python]
      - id: ruff-format
        types: [python]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0 # Use the latest stable version
    hooks:
      - id: no-commit-to-branch
        args: ["--branch", "main", "--branch", "master"]
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-merge-conflict
      - id: check-toml
      - id: check-yaml
      - id: debug-statements
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks
  - repo: local
    hooks:
      - id: pip-audit
        name: pip-audit
        entry: uv run --with pip-audit pip-audit --skip-editable
        language: system
        pass_filenames: false
        files: (^pyproject\.toml$|^uv\.lock|^.pre-commit-config.yaml$)
      - id: lint-textio
        name: lint text IO encoding
        entry: uv run python scripts/lint_textio.py --verbose
        language: system
        types: [python]
        # 'src/' only: the test suite writes its own fixtures, which are
        # not UTF-8 by specification (mirrors soliplex's scoping).
        files: ^src/soliplex_plumber/
      - id: lint-textio-self-test
        name: lint text IO encoding (self-test)
        entry: uv run python scripts/lint_textio.py --self-test
        language: system
        pass_filenames: false
        files: ^scripts/lint_textio\.py$

  - repo: https://github.com/jackdewinter/pymarkdown
    rev: v0.9.36
    hooks:
      - id: pymarkdown
        args:
          - "scan"
        exclude: ^ios/Runner/Assets\.xcassets/

  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.7
    hooks:
      - id: actionlint
